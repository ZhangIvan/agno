# 多模态知识库功能更新说明

## 与 Doubao 多模态向量模型的兼容性

当前更新**完全兼容** `doubao-embedding-vision` 系列模型（豆包 / 火山引擎方舟平台）的调用方式。

文档中描述的 API 核心调用逻辑：
```python
client.multimodal_embeddings.create(
    model="doubao-embedding-vision-251215",
    encoding_format="float",
    input=[{"type": "image_url", "image_url": {"url": "data:image/png;base64,..."}}]
)
```
在新增的 `DoubaoEmbedder` 中已完整实现，包含文本和图片两种输入方式，以及同步/异步两套接口。

---

## 更新文件清单

| 文件路径 | 操作 | 说明 |
|---|---|---|
| `libs/agno/agno/knowledge/reader/page_capture.py` | **新建** | 文档截图工具函数 |
| `libs/agno/agno/knowledge/reader/pdf_reader.py` | **修改** | 添加 `capture_pages` 选项 |
| `libs/agno/agno/knowledge/reader/pptx_reader.py` | **修改** | 改为按幻灯片分文档 + `capture_pages` |
| `libs/agno/agno/knowledge/reader/docx_reader.py` | **修改** | 添加 `capture_pages` 选项（LibreOffice 转换） |
| `libs/agno/agno/knowledge/embedder/base.py` | **修改** | 新增图片 embedding 接口 |
| `libs/agno/agno/knowledge/embedder/doubao.py` | **新建** | Doubao 多模态 Embedder |
| `libs/agno/agno/knowledge/embedder/aws_bedrock.py` | **修改** | 支持文件路径输入 |
| `libs/agno/agno/knowledge/document/base.py` | **修改** | `embed()` 支持图片文档 |
| `libs/agno/agno/knowledge/knowledge.py` | **修改** | 滑动窗口图片检索 + `ToolResult` 返回 |
| `cookbook/knowledge/multimodal_pdf_knowledge.py` | **新建** | 示例 cookbook |

---

## 各文件更新详情

### 1. `page_capture.py`（新建）

**位置**：`libs/agno/agno/knowledge/reader/page_capture.py`

**功能**：文档页面截图工具，将 PDF/PPTX/DOCX 的每一页/幻灯片渲染为 PNG 图片。

**核心函数**：

| 函数 | 说明 |
|---|---|
| `capture_pdf_pages(pdf_path, output_dir, dpi=150)` | 用 `pymupdf` 渲染 PDF 每页为 PNG |
| `capture_pptx_slides(pptx_path, output_dir, dpi=150)` | LibreOffice 将 PPTX 转为 PDF，再调用上面的函数 |
| `capture_docx_pages(docx_path, output_dir, dpi=150)` | LibreOffice 将 DOCX 转为 PDF，再调用上面的函数 |
| `get_page_cache_dir(base_dir, doc_name)` | 返回/创建每个文档的图片缓存目录 |

返回值格式：`{1: "/path/page_1.png", 2: "/path/page_2.png", ...}`（1-based 页码 → 文件路径）

**依赖**：
- `pymupdf`（无系统依赖，`pip install pymupdf`）
- LibreOffice headless（DOCX/PPTX 转换，需系统安装）

---

### 2. `pdf_reader.py`（修改）

**新增参数**（`BasePDFReader.__init__`）：

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `capture_pages` | `bool` | `False` | 是否截图每页 |
| `pages_cache_dir` | `Optional[str]` | `None` | 图片缓存目录（默认 `~/.agno/page_cache/`） |
| `image_dpi` | `int` | `150` | 截图 DPI |

**`_create_documents()` 逻辑变化**：

启用 `capture_pages` 时，除了原有文本 Document，还会：
1. 在每个文本块的 `meta_data` 中写入：
   - `page_number`: 页码（1-based）
   - `total_pages`: 总页数
   - `page_image_path`: 对应页面的 PNG 路径
   - `doc_type`: `"text_chunk"`
   - `pages_cache_dir`: 图片缓存目录（用于相邻页查找）
2. **额外追加图片 Document**（每页一个）：
   - `content = ""`（空文本，由图片 embedding 代替）
   - `meta_data["doc_type"] = "page_image"`
   - 该文档会用图片 embedding 向量化后存入 vectordb

---

### 3. `pptx_reader.py`（修改）

**行为变化**：之前所有幻灯片合并为一个大文档，现在**每张幻灯片单独成一个 Document**。

- 每个 Document 的 `meta_data` 含 `page_number`（幻灯片编号）和 `total_pages`
- 新增 `capture_pages`、`pages_cache_dir`、`image_dpi` 参数
- 启用截图时，调用 `capture_pptx_slides()` 并将图片路径写入 metadata，同时追加图片 Document

---

### 4. `docx_reader.py`（修改）

- 新增 `capture_pages`、`pages_cache_dir`、`image_dpi` 参数
- 启用截图时：调用 `capture_docx_pages()`（LibreOffice 路径） → 获得页面截图
- 由于 python-docx 无法直接感知页面边界，通过**比例映射**将文本块分配到对应页码：`approx_page = round((chunk_index / total_chunks) * total_pages) + 1`

---

### 5. `embedder/base.py`（修改）

新增两个方法，默认返回 `None`（不支持时不报错，由文本 embedding 兜底）：

```python
def get_image_embedding(self, image_path: str) -> Optional[List[float]]:
    """获取图片的向量表示。不支持时返回 None。"""
    return None

async def async_get_image_embedding(self, image_path: str) -> Optional[List[float]]:
    return None
```

子类通过覆写此方法来支持图片向量化。

---

### 6. `embedder/doubao.py`（新建）

**完整实现 Doubao `doubao-embedding-vision` 系列多模态向量模型**。

**参数**：

| 参数 | 默认值 | 说明 |
|---|---|---|
| `id` | `"doubao-embedding-vision-251215"` | 模型 ID |
| `dimensions` | `None` | 向量维度（仅 250615 及后续支持） |
| `encoding_format` | `"float"` | 编码格式 |
| `api_key` | `None` | ARK API Key，默认读取 `ARK_API_KEY` 或 `DOUBAO_API_KEY` 环境变量 |
| `instructions` | `None` | 任务指令（仅 251215 及后续支持） |

**接口实现**：

| 方法 | 输入 | 说明 |
|---|---|---|
| `get_embedding(text)` | 文本字符串 | 调用 `multimodal_embeddings.create()` with `type="text"` |
| `get_embedding_and_usage(text)` | 文本字符串 | 同上，附带 token 用量 |
| `async_get_embedding(text)` | 文本字符串 | 异步版本 |
| `async_get_embedding_and_usage(text)` | 文本字符串 | 异步版本附用量 |
| `get_image_embedding(image_path)` | 文件路径/URL/base64 | 调用 `multimodal_embeddings.create()` with `type="image_url"` |
| `async_get_image_embedding(image_path)` | 文件路径/URL/base64 | 异步版本 |

**图片输入格式兼容**：
- 本地文件路径 → 自动转为 `data:image/<format>;base64,...`
- 远程 URL（`http://...`）→ 直接传给 API
- Base64 data URI（`data:...`）→ 直接传给 API

**支持模型版本对比**：

| 模型 | 文本 | 图片 | 视频 | `dimensions` | `instructions` |
|---|---|---|---|---|---|
| `doubao-embedding-vision-241215` | ✓ | ✓ | ✗ | ✗（固定 3072） | ✗ |
| `doubao-embedding-vision-250328` | ✓ | ✓ | ✗ | ✗（最高 2048） | ✗ |
| `doubao-embedding-vision-250615` | ✓ | ✓ | ✓ | ✓ | ✗ |
| `doubao-embedding-vision-251215` | ✓ | ✓ | ✓ | ✓ | ✓ |

---

### 7. `embedder/aws_bedrock.py`（修改）

新增静态方法 `_to_image_data_uri(image_path_or_uri: str) -> str`：
- 如果传入的是本地文件路径，自动读取并编码为 `data:image/...;base64,...`
- 如果已经是 data URI，直接透传

`get_image_embedding()` 和 `async_get_image_embedding()` 现在都先调用此方法，因此**支持直接传入本地文件路径**，与 `DoubaoEmbedder` 接口保持一致。

---

### 8. `document/base.py`（修改）

`embed()` 和 `async_embed()` 新增图片文档识别逻辑：

```python
# 图片文档：优先用图片 embedding
if self.meta_data.get("doc_type") == "page_image":
    image_path = self.meta_data.get("page_image_path")
    if image_path:
        img_embedding = _embedder.get_image_embedding(image_path)
        if img_embedding is not None:
            self.embedding = img_embedding
            return   # 不再走文本 embedding

# 默认：文本 embedding
self.embedding, self.usage = _embedder.get_embedding_and_usage(self.content)
```

逻辑：`doc_type == "page_image"` 的文档调用 `get_image_embedding()`；若 embedder 不支持图片（返回 `None`），自动退回文本 embedding（兜底策略）。

---

### 9. `knowledge.py`（修改）

**新增 Knowledge 类参数**：

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `use_page_images` | `bool` | `False` | 开启图片检索模式 |
| `max_retrieval_images` | `int` | `6` | 每次检索最多提交多少张图片 |
| `image_window_size` | `int` | `1` | 滑动窗口大小：±N 页 |

**新增方法**：

`_resolve_page_image_path(doc, page_num)` — 解析指定页码的图片路径：
1. 优先从 doc.meta_data["page_image_path"] 读取（当前页）
2. 退回从 meta_data["pages_cache_dir"] 推算相邻页路径
3. 最终退回默认缓存目录 `~/.agno/page_cache/<doc_name>/page_N.png`

`_get_page_images_for_docs(docs)` — 滑动窗口收集图片：
- 遍历所有检索到的 Document
- `doc_type == "page_image"`：只取该页图片
- `doc_type == "text_chunk"`：取 `[page-window, ..., page+window]` 范围内的图片
- 按 `(doc_id, page_num)` 去重，按顺序排列，截取前 `max_retrieval_images` 张
- 返回 `List[agno.media.Image]`

**搜索工具返回值变化**（4 个 search tool 内部函数统一修改）：

```python
# 启用图片模式时返回 ToolResult（图片 + 文本摘要）
if self.use_page_images and docs:
    images = self._get_page_images_for_docs(docs)
    if images:
        return ToolResult(
            content=f"Found {len(docs)} relevant document sections across {len(images)} pages.",
            images=images,
        )

# 默认退回纯文本
return self._convert_documents_to_string(docs, agent)
```

`ToolResult` 是 agno 已有的数据结构（`libs/agno/agno/tools/function.py`），model 层会自动将 `images` 字段作为 vision 内容注入 LLM 消息。

---

## 完整数据流

### 写入阶段（文档入库）

```
用户调用 knowledge.insert(path="doc.pdf")
    │
    ├─ PDFReader(capture_pages=True).read("doc.pdf")
    │       │
    │       ├─ 用 pypdf 逐页提取文本
    │       ├─ 用 pymupdf 渲染每页为 PNG → 写入 ~/.agno/page_cache/doc/page_N.png
    │       │
    │       ├─ 文本 Document × N 页
    │       │   meta_data: {page_number, total_pages, page_image_path, doc_type="text_chunk"}
    │       │
    │       └─ 图片 Document × N 页
    │           content="", meta_data: {doc_type="page_image", page_image_path}
    │
    ├─ 对每个 Document 调用 embed()
    │       │
    │       ├─ doc_type == "text_chunk"  → DoubaoEmbedder.get_embedding(text)
    │       │       → POST /embeddings/multimodal  input=[{type:"text"}]
    │       │       → 返回 List[float]
    │       │
    │       └─ doc_type == "page_image"  → DoubaoEmbedder.get_image_embedding(image_path)
    │               → 读取 PNG → base64 data URI
    │               → POST /embeddings/multimodal  input=[{type:"image_url"}]
    │               → 返回 List[float]
    │
    └─ vectordb.insert(所有 Document)   ← 文本块 + 图片块都进 vectordb
```

### 检索阶段（Agent 查询）

```
用户向 Agent 提问
    │
    └─ Agent 调用 search_knowledge_base(query)
            │
            ├─ DoubaoEmbedder.get_embedding(query)
            │       → POST /embeddings/multimodal  input=[{type:"text"}]
            │
            ├─ vectordb.search(query_vector, limit=10)
            │       → 返回 List[Document]（可能混合 text_chunk 和 page_image）
            │
            ├─ Knowledge._get_page_images_for_docs(docs)
            │       │
            │       ├─ 对每个命中的 doc：
            │       │   ├─ page_image doc  → 直接取该页图片
            │       │   └─ text_chunk doc  → 取 [page-1, page, page+1] 共 3 页图片
            │       │
            │       ├─ 去重（按 doc_id + page_num）
            │       ├─ 排序（doc 顺序 → 页码顺序）
            │       └─ 截取前 max_retrieval_images(=6) 张
            │
            └─ 返回 ToolResult(content="Found N sections...", images=[Image, Image, ...])
                    │
                    └─ model 层将 images 注入 LLM 消息（视觉内容）
                            │
                            └─ LLM 直接看到页面图片，做出回答
```

### 滑动窗口示例

假设命中 2 个文本块（doc_A 第 3 页，doc_A 第 7 页），`image_window_size=1`，`max_retrieval_images=6`：

```
命中块 1: doc_A, page=3  → 收集 page 2, 3, 4
命中块 2: doc_A, page=7  → 收集 page 6, 7, 8

合并去重后: [page 2, 3, 4, 6, 7, 8]  共 6 张 ≤ 6（上限）
向 LLM 提交 6 张图片
```

---

## 快速使用示例

```python
import os
os.environ["ARK_API_KEY"] = "your_api_key"

from agno.agent.agent import Agent
from agno.knowledge.embedder.doubao import DoubaoEmbedder
from agno.knowledge.knowledge import Knowledge
from agno.knowledge.reader.pdf_reader import PDFReader
from agno.models.anthropic.claude import Claude
from agno.vectordb.pgvector.pgvector import PgVector

# 1. 构建多模态 Knowledge
knowledge = Knowledge(
    vector_db=PgVector(
        table_name="my_kb",
        db_url="postgresql+psycopg://ai:ai@localhost:5532/ai",
        embedder=DoubaoEmbedder(id="doubao-embedding-vision-251215", dimensions=1024),
    ),
    use_page_images=True,      # 开启图片检索
    max_retrieval_images=6,    # 最多提交 6 张图片
    image_window_size=1,       # 命中页 ±1 页
)

# 2. 写入文档（会自动截图）
reader = PDFReader(capture_pages=True, image_dpi=150)
knowledge.insert(path="your_document.pdf", reader=reader, upsert=True)

# 3. 创建 Agent
agent = Agent(
    model=Claude(id="claude-opus-4-6"),
    knowledge=knowledge,
    search_knowledge=True,
)

# 4. 提问，LLM 将收到页面图片而非文本块
agent.print_response("文档第一章主要讲了什么？", stream=True)
```

---

## 依赖安装

```bash
# 核心依赖（PDF 截图）
pip install pymupdf

# Doubao 向量模型
#pip install volcenginesdkarkruntime
pip install  'volcengine-python-sdk[ark]'

# AWS Bedrock（可选替代）
pip install boto3

# LibreOffice（DOCX/PPTX 截图，系统级安装）
# Ubuntu/Debian:
sudo apt-get install libreoffice
# macOS:
brew install libreoffice
# Win 手动安装后，环境变量配置
# - https://zh-cn.libreoffice.org/download/libreoffice/
```
